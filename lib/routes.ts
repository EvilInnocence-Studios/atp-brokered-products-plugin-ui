import { BrokerageManager } from "@brokered-products-plugin/components/BrokerageManager";

export const brokerageRoutes = {
    admin: [
        {path: "/brokerages", component: BrokerageManager},
    ]
}